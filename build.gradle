buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.7.+'
    }
}

ext.groupId = 'eu.szwiec.messagebar'

allprojects {
    version = '1.0.3-SNAPSHOT'
}
